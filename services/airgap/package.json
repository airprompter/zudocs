{
  "name": "@zudocs/airgap",
  "version": "0.1.0",
  "private": true,
  "description": "The Zudocs air-gapped host in ap-southeast-1: what runs on a t4g.micro with no route out — the Agent SDK offline on a vendored bundle, applying each newer bundle from the exchange bucket, its distribution keypair born on the host, its status document and telemetry exports written back to the bucket — with the boot script, the units and the helpers.",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@airprompter/agent-sdk": "0.2.14",
    "@aws-sdk/client-dynamodb": "3.1135.0",
    "@aws-sdk/client-s3": "3.1135.0",
    "@aws-sdk/credential-provider-node": "3.972.83",
    "@aws-sdk/lib-dynamodb": "3.1135.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "esbuild": "0.28.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
